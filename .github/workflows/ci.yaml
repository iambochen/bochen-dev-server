name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

concurrency:
  group:
    ${{ github.workflow }}-${{ github.ref_name }}-${{
    github.event.pull_request.number || github.sha }}
  cancel-in-progress: true

jobs:
  build:
    name: "build image"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and export
        uses: docker/build-push-action@v6
        with:
          tags: bochen-dev-server:latest
          cache-from: type=gha,scope=bochen-dev-server
          cache-to: type=gha,mode=min,scope=bochen-dev-server
          outputs: type=docker,dest=/tmp/bochen-dev-server.tar

      - name: Build and export (multistage)
        uses: docker/build-push-action@v6
        with:
          file: multistage.Dockerfile
          tags: bochen-dev-server-multistage:latest
          cache-from: type=gha,scope=bochen-dev-server-multistage
          cache-to: type=gha,mode=min,scope=bochen-dev-server-multistage
          outputs: type=docker,dest=/tmp/bochen-dev-server-multistage.tar

      - name: Build and export (standalone)
        uses: docker/build-push-action@v6
        with:
          file: standalone.Dockerfile
          tags: bochen-dev-server-standalone:latest
          cache-from: type=gha,scope=bochen-dev-server-standalone
          cache-to: type=gha,mode=min,scope=bochen-dev-server-standalone
          outputs: type=docker,dest=/tmp/bochen-dev-server-standalone.tar

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: bochen-dev-server
          path: /tmp/bochen-dev-server.tar

      - name: Upload artifact (multistage)
        uses: actions/upload-artifact@v4
        with:
          name: bochen-dev-server-multistage
          path: /tmp/bochen-dev-server-multistage.tar

      - name: Upload artifact (standalone)
        uses: actions/upload-artifact@v4
        with:
          name: bochen-dev-server-standalone
          path: /tmp/bochen-dev-server-standalone.tar

  test:
    name: "test image"
    runs-on: ubuntu-latest
    needs: build
    steps:
      - uses: actions/checkout@v4

      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: bochen-dev-server
          path: /tmp

      - name: Download artifact (multistage)
        uses: actions/download-artifact@v4
        with:
          name: bochen-dev-server-multistage
          path: /tmp

      - name: Download artifact (standalone)
        uses: actions/download-artifact@v4
        with:
          name: bochen-dev-server-standalone
          path: /tmp
  
      - name: Load images
        run: |
          docker load --input /tmp/bochen-dev-server.tar
          docker load --input /tmp/bochen-dev-server-multistage.tar
          docker load --input /tmp/bochen-dev-server-standalone.tar
          docker image ls -a

      - name: Test command line
        run: ./run.sh hello

      - name: Test Docker compose
        run: |
          docker compose up -d
          docker compose down

      - name: Test command line (multistage)
        run: docker run bochen-dev-server-multistage:latest hello

      - name: Test command line (standalone)
        run: docker run bochen-dev-server-standalone:latest hello
